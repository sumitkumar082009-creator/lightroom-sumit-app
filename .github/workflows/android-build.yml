name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Validate Gradle wrapper
      uses: gradle/wrapper-validation-action@v1

    - name: Set up Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'

    - name: Cache Gradle directories
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*','**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Ensure Gradle wrapper is executable
      run: chmod +x ./gradlew

    - name: Build Debug APK
      run: ./gradlew assembleDebug --stacktrace

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Sumit-Lightroom-App
        path: app/build/outputs/apk/debug/*.apk

# Optional: Signed release build (uncomment & configure secrets to use)
#  - name: Decode keystore
#    if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
#    run: |
#      echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > release.keystore
#
#  - name: Create gradle properties for signing
#    if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
#    run: |
#      cat >> $GITHUB_WORKSPACE/gradle.properties <<EOF
#      ORG_GRADLE_PROJECT_keystoreFile=$GITHUB_WORKSPACE/release.keystore
#      ORG_GRADLE_PROJECT_keystorePassword=${{ secrets.KEYSTORE_PASSWORD }}
#      ORG_GRADLE_PROJECT_keyAlias=${{ secrets.KEY_ALIAS }}
#      ORG_GRADLE_PROJECT_keyPassword=${{ secrets.KEY_PASSWORD }}
#      EOF
#
#  - name: Build Release APK
#    if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
#    run: ./gradlew assembleRelease --stacktrace
#
#  - name: Upload Release APK
#    if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
#    uses: actions/upload-artifact@v4
#    with:
#      name: Sumit-Lightroom-App-Release
#      path: app/build/outputs/apk/release/*.k 
